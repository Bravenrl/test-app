export enum Reducer {
  User = 'USER',
  Data = 'DATA',
  Process = 'PROCESS',
}

export enum Slice {
  AppUser = 'user',
  AppData = 'data',
  AppProcess = 'process',
}

export enum AsyncThunk {
  Login = 'user/login',
  Registration = 'user/registration',
}
