export enum Reducer {
    User = 'USER',
    Data = 'DATA', 
  }
  
  export enum Slice {
    AppUser = 'user',
    AppData = 'data',
  }
  
  export enum AsyncThunk {
    Login = 'user/login',
    Registration = 'user/registration',
  }